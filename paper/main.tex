\documentclass[sigconf,anonymous]{acmart}

%% CBSoft uses an ACM-like format without the ACM Reference block.
%% The following warning from acmart can be safely ignored:
%% "ACM reference format is mandatory for papers over one page."
\settopmatter{printacmref=false}
\renewcommand\footnotetextcopyrightpermission[1]{} % removes footnote with conference information in first column

%% CBSoft uses an ACM-like format without a copyright note.
\setcopyright{none}

%% SBES 2026 conference metadata
\acmConference[SBES 2026]{40th Brazilian Symposium on Software Engineering}{September 8--12, 2026}{São Paulo, SP, Brazil}

%% BibTeX command
\AtBeginDocument{
    \providecommand\BibTeX{{Bib\TeX}}
}

%% Paper written in ENGLISH — keep default Abstract/Keywords/References labels
%% If switching to Portuguese, uncomment the following three lines:
%\renewcommand\abstractname{Resumo}
%\renewcommand\keywordsname{Palavras-chave}
%\renewcommand\refname{Referências}

%% Additional packages
\usepackage{booktabs}   % For high-quality tables
\usepackage{multirow}   % For multi-row table cells
\usepackage{xcolor}     % For colored text (e.g., TODO markers)
\usepackage{listings}   % For code listings if needed

%% TODO command for draft annotations (remove before submission)
\newcommand{\TODO}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}

% ============================================================
\begin{document}

%% Title
\title{How Do AI Agents Resolve Merge Conflicts? An Empirical Study of Conflict Resolution Strategies in AI-Authored Pull Requests}

%% Authors (hidden during double-anonymous review)
\author{FirstName Surname}
\affiliation{
  \institution{Department, University}
  \city{City}
  \country{Country}
}
\email{email@email.com}

\author{FirstName Surname}
\affiliation{
  \institution{Department, University}
  \city{City}
  \country{Country}
}
\email{email@email.com}

%% Short author list for page headers
\renewcommand{\shortauthors}{Surname et al.}
\renewcommand{\shorttitle}{How Do AI Agents Resolve Merge Conflicts?}

% ============================================================
% Abstract
% ============================================================
\begin{abstract}
AI-powered coding agents such as Claude Code, OpenAI Codex, GitHub Copilot, Cursor, and Devin are increasingly contributing to open-source software development through pull requests (PRs). When these AI-authored branches incorporate upstream changes via merge commits, textual merge conflicts can arise and must be resolved---either by the AI agent itself or by a human collaborator. Despite the growing adoption of agentic AI in software development, little is known about how merge conflicts are resolved in this context: which strategies are employed, who performs the resolution, and what factors influence the choice of strategy. In this paper, we present a large-scale empirical study of merge conflict resolution in AI-authored PRs, building upon the AgenticFlict dataset of 142,652 PRs opened by five AI agents across 59,412 repositories. By reproducing real merge commits found within PR branches using three-way \texttt{diff3} merging and comparing the conflicting regions against the actual resolved files, we classify each conflict chunk into one of eight resolution strategies (V1, V2, ConcatV1V2, ConcatV2V1, Combination, New~Code, None, Imprecise) and identify whether the resolver was an AI agent or a human developer. Our results show \TODO{summarize key findings}. We discuss the implications of these findings for the design of AI-assisted merge tools and for understanding the collaborative dynamics between AI agents and human developers.
\end{abstract}

%% Keywords
\keywords{Merge Conflicts, AI Agents, Conflict Resolution, Software Merging, Empirical Software Engineering}

\frenchspacing
\maketitle

% ============================================================
% 1. Introduction
% ============================================================
\section{Introduction}
\label{sec:introduction}

% Qual é o contexto em que esse trabalho está inserido?
Concurrent work is essential to large-scale software development.
Once developers finish their independent work, changes must be integrated and made available to other developers, typically through a merge tool that aims to automate as much as possible of the task of combining parallel changes~\cite{mens2002}.
Despite decades of research on merge techniques, none of them can fully automate the process: when concurrent modifications affect overlapping regions, the tool produces a \emph{merge conflict} that a developer must inspect, understand, and resolve manually~\cite{ghiotto2020nature, brun2011}.
It has been reported that 10 to 20 percent of all merge attempts result in a conflict~\cite{ghiotto2020nature, kasi2013}, and that resolving them is a difficult, time-consuming, and error-prone task~\cite{vale2022challenges, nelson2019}.
Surveys with developers reveal that 56\% of them have deferred resolving a conflict due to its complexity, the large size of the conflicting code, or the large number of conflicting locations~\cite{nelson2019}, and that factors such as the number of conflicting chunks, lines of code, and code complexity significantly influence the time required for resolution~\cite{vale2022challenges}.
To date, the most comprehensive characterization of merge conflicts in the human development context was provided by Ghiotto et al.~\cite{ghiotto2020nature}, who examined over 960,000 merges across 2,731 open-source Java projects and established a multi-dimensional empirical portrait of conflicting chunks, covering the number of chunks per failed merge, chunk size in lines of code, the resolution strategies chosen by developers (V1, V2, Concatenation, Combination, New Code, and None), and the relationships between conflict characteristics and resolution decisions.
A key finding of that study is that 87\% of the conflict resolutions are composed exclusively of lines already present in the conflicting versions, without introducing any new code~\cite{ghiotto2020nature}. This result, independently confirmed by subsequent work~\cite{dinella2022deepmerge, boll2024derivability, campos2022combining}, has become a central assumption for virtually all automated resolution techniques proposed since.

% Qual é o problema que motiva a existência desse trabalho?
Software engineering is entering a new era shaped by autonomous coding agents, such as OpenAI Codex, Devin, GitHub Copilot, Cursor, and Claude Code, that go beyond code completion to autonomously create branches, implement features, and open pull requests (PRs) alongside human developers~\cite{li2025aidev}.
This shift is no longer speculative: the AIDev dataset documents over 456,000 agent-authored PRs across more than 61,000 repositories, with OpenAI Codex alone producing over 400,000 PRs in less than two months since its release~\cite{li2025aidev, li2026aidev_msr}.
When an agent-authored branch incorporates upstream changes (for instance via \texttt{git merge main}), textual merge conflicts may arise just as they do in human-driven development.
However, we do not know whether the nature of the conflicts generated by agent-authored PRs resembles the nature of the conflicts generated by humans.
This question is urgent for three reasons.
First, existing automated resolution techniques were designed on top of Ghiotto et al.'s human-centric characterization, and their validity for agent-generated conflicts is an untested assumption.
Second, every conflict that the agent does not resolve falls back to a human developer, increasing the coordination overhead of agent-driven development.
Third, the AIDev literature itself lists the treatment of merge conflicts by coding agents as an open research direction~\cite{li2025aidev}.

% Como a literatura tratou esse problema até então e por que a solução dada pela literatura não foi suficiente?
Prior work on merge conflict resolution has focused exclusively on human developers.
Ghiotto et al.~\cite{ghiotto2020nature} characterized the nature of human-authored conflicts, and their findings have served as the empirical substrate on top of which a rich body of subsequent research has been conceived.
On the resolution side, approaches range from structured and semi-structured merge techniques that exploit the syntactic structure of programs~\cite{cavalcanti2019impact} to data-driven methods such as DeepMerge~\cite{dinella2022deepmerge} and MergeBERT~\cite{svyatkovskiy2022mergebert}, which learn resolution patterns from historical merge data, and MergeGen~\cite{dong2023mergegen}, which reframes resolution as a generation task rather than a classification task.
More recent work has applied large language models to generate resolutions~\cite{shen2023chatmerge} and search-based software engineering to explore the combinatorial space of candidate solutions~\cite{campos2025evaluation}.
On the prediction side, tools such as MESTRE~\cite{elias2023mestre} and RPredictor~\cite{aldndni2023rpredictor} use machine learning to predict which resolution strategy a developer is likely to adopt, while Boll et al.~\cite{boll2024derivability} studied the derivability of resolutions across 131,154 conflicting merge commits from 10,000 projects and found that 87.9\% of individual chunks can be derived from a fixed set of language-agnostic patterns.
All of these efforts share a common trait: they were designed, trained, and evaluated exclusively on conflicts produced by human developers.
On the agent side, the AIDev dataset~\cite{li2025aidev, li2026aidev_msr} cataloged hundreds of thousands of agent-authored PRs with rich metadata on authorship, review timelines, and task types, but does not provide conflict-level analysis.
More recently, Ogenrwot and Businge~\cite{ogenrwot2026agenticflict} introduced \textsc{AgenticFlict}, a large-scale dataset of 142,652 agent PRs processed through deterministic merge simulation, identifying approximately 29,000 PRs with conflicts and extracting over 336,000 fine-grained conflict regions.
While \textsc{AgenticFlict} represents an important step toward understanding integration friction in AI-assisted development, the dataset focuses on conflict \emph{detection} (recording conflict markers and line spans) rather than on characterizing the \emph{resolution}: which strategies are employed, what the structural properties of conflicts and resolutions are, and who performs the resolution.
To the best of our knowledge, no study has yet characterized the nature of merge conflicts in the agent-authored regime along the dimensions established by Ghiotto et al.

% Como o seu trabalho trata o problema?
In this paper, we replicate and adapt Ghiotto et al.'s analytical framework over AIDev-pop, the curated subset of AIDev restricted to repositories with more than 100 stars, comprising approximately 33,600 PRs across 2,807 repositories~\cite{li2026aidev_msr}.
Unlike \textsc{AgenticFlict}, which simulates the merge between the PR head and base branches, our study targets the \emph{internal merge commits} within PR branches, that is, merge commits created when the agent (or a human collaborator) pulls upstream changes into the feature branch.
For each such merge, we replay the three-way merge between its parents, extract every conflicting chunk, localize the resolution actually adopted in the merge commit, and classify it into one of the canonical strategies (V1, V2, Concatenation, Combination, New Code, None).
We further identify whether the conflict was resolved by the coding agent or by a human developer.
Specifically, we address the following research questions:

\begin{itemize}
    \item \textbf{RQ1 (Structural nature).} What are the distributions of the number of conflicting chunks per failed merge, the size of each version (\textit{v1}, \textit{v2}), and the size of the resolution?
    \item \textbf{RQ2 (Resolution strategies).} How are resolution decisions distributed across conflicting chunks, and how does this distribution compare to the one reported by Ghiotto et al.\ for human-authored merges?
    \item \textbf{RQ3 (Resolver).} Who resolves these conflicts: the coding agent itself or a human collaborator?
\end{itemize}

For every research question in which it is meaningful, results are stratified by coding agent, programming language, and PR task type.

% Quais foram os resultados obtidos nessa avaliação?
\TODO{Summarize key findings once the data extraction is complete.}

% Como o seu artigo está organizado?
The remainder of this paper is organized as follows.
Section~\ref{sec:background} presents background on software merging, merge conflict resolution strategies, and AI coding agents.
Section~\ref{sec:methodology} describes the methodology used to collect, replay, and classify merge conflicts.
Section~\ref{sec:results} presents our results, organized by research question.
Section~\ref{sec:discussion} discusses the implications of our findings.
Section~\ref{sec:threats} covers threats to validity, and Section~\ref{sec:conclusion} concludes with future work directions.

% ============================================================
% 2. Background and Related Work
% ============================================================
\section{Background and Related Work}
\label{sec:background}

\subsection{Software Merging and Merge Conflicts}
\label{sec:bg-merging}

\TODO{Explain three-way merge, diff3 format, conflict markers. Cite foundational work on merge conflicts (e.g., Mens 2002, Brun et al. 2011, Accioly et al. 2018).}

\subsection{Merge Conflict Resolution Strategies}
\label{sec:bg-strategies}

\TODO{Discuss prior taxonomies of resolution strategies. Explain the eight categories we adopt: V1, V2, ConcatV1V2, ConcatV2V1, Combination, New Code, None, Imprecise. Cite DeepMerge (Dinella et al. 2022) and related work.}

\subsection{AI Agents in Software Development}
\label{sec:bg-agents}

\TODO{Overview of the five agents under study (Claude Code, Codex, Copilot, Cursor, Devin). Discuss the growing role of AI in OSS contributions.}

\subsection{The AgenticFlict Dataset}
\label{sec:bg-agenticflict}

\TODO{Summarize the AgenticFlict dataset: 142,652 PRs, 59,412 repositories, five agents. Explain its limitations regarding conflict resolution (only simulated conflicts, no resolution data). Position our study as an extension.}

% ============================================================
% 3. Methodology
% ============================================================
\section{Methodology}
\label{sec:methodology}

Figure~\ref{fig:pipeline} provides an overview of our data collection and analysis pipeline.

\TODO{Create and include a pipeline figure.}

%\begin{figure}[ht]
%  \centering
%  \includegraphics[width=\linewidth]{figures/pipeline}
%  \caption{Overview of the data collection and analysis pipeline.}
%  \Description{A flowchart showing the five stages of the pipeline: PR selection, merge listing, conflict extraction, authorship collection, and strategy classification.}
%  \label{fig:pipeline}
%\end{figure}

\subsection{PR Selection and Scope}
\label{sec:meth-selection}

Our study leverages the AgenticFlict dataset as its starting point. We consider all 142,652 PRs authored by AI agents for which both \texttt{base\_oid} and \texttt{head\_oid} are available. For each PR, we classify its outcome (\textit{merged}, \textit{closed\_unmerged}, or \textit{open}) based on the \texttt{merged\_at} and \texttt{gh\_state} fields.

\TODO{Report the actual number of PRs after filtering.}

\subsection{Identifying Merge Commits Within PR Branches}
\label{sec:meth-merges}

For each PR, we identify merge commits (commits with exactly two parents) that exist within the PR branch but not in the base branch:

\begin{verbatim}
git rev-list --merges base_oid..head_oid
\end{verbatim}

These are merges that the PR author (or collaborators) created to bring upstream changes into the feature branch---typically via \texttt{git merge main}. We exclude octopus merges (more than two parents) and do not consider the integration merge created by GitHub when the PR is accepted.

\TODO{Report the number of merge commits found and the fraction of PRs that contain at least one merge commit.}

\subsection{Reproducing Merges and Extracting Conflict Chunks}
\label{sec:meth-conflicts}

For each merge commit with parents $p_1$ and $p_2$, we compute the merge base ($b = \text{merge-base}(p_1, p_2)$) and identify files modified on both sides using \texttt{git merge-tree}. For each such file, we reproduce the three-way merge using:

\begin{verbatim}
git merge-file -p --diff3 p1_file base_file p2_file
\end{verbatim}

We parse the resulting diff3 output to extract individual conflict chunks, each delimited by \texttt{<<<<<<<}, \texttt{|||||||}, \texttt{=======}, and \texttt{>>>>>>>} markers. Each chunk produces three text regions: \textit{v1} (from $p_1$), \textit{base} (from $b$), and \textit{v2} (from $p_2$).

\subsection{Localizing the Resolution}
\label{sec:meth-resolution}

For each conflict chunk, we retrieve the resolved file from the merge commit itself via \texttt{git show \{merge\_sha\}:\{file\_path\}}. Since this file contains the entire contents (including non-conflicting regions and other resolutions), we isolate the specific resolution text for each chunk using contextual anchoring: we identify the unique prefix and suffix surrounding the conflict region in the merged output and use them to extract the corresponding slice from the resolved file.

\TODO{Report the proportion of chunks with successfully localized resolutions vs.\ not\_localizable, file\_removed, etc.}

\subsection{Classifying Resolution Strategies}
\label{sec:meth-strategies}

We classify each resolved chunk into one of eight resolution strategies using the \texttt{identify\_resolution} function, which compares the normalized resolution text against \textit{v1} and \textit{v2}:

\begin{itemize}
    \item \textbf{V1} / \textbf{V2}: The resolution is identical to one side of the conflict.
    \item \textbf{ConcatV1V2} / \textbf{ConcatV2V1}: The resolution is the concatenation of both sides, in the given order.
    \item \textbf{Combination}: The resolution contains elements from both sides but is not a simple concatenation.
    \item \textbf{New Code}: The resolution introduces content not present in either side.
    \item \textbf{None}: Both sides and the resolution are empty.
    \item \textbf{Imprecise}: The resolution could not be reliably classified.
\end{itemize}

For the statistical analysis in RQ4, we also define an aggregated four-level taxonomy: \textit{take-one-side} (V1, V2), \textit{combine} (ConcatV1V2, ConcatV2V1, Combination), \textit{introduce-new} (New Code), and \textit{trivial-or-other} (None, Imprecise).

\subsection{Identifying the Resolver}
\label{sec:meth-resolver}

We determine who resolved each conflict by analyzing the author and committer metadata of the merge commit. We classify the resolver as \textit{AI}, \textit{Human}, or \textit{Unknown} based on a curated list of known AI agent signatures (e.g., \texttt{claude[bot]}, \texttt{openai-codex[bot]}, \texttt{copilot[bot]}, \texttt{cursor-agent[bot]}, \texttt{devin-ai-integration[bot]}).

\subsection{Textual Similarity Metrics}
\label{sec:meth-similarity}

For each resolved chunk, we compute the Gestalt pattern matching (longest common subsequence) similarity between the resolution and each side of the conflict (\textit{sim\_res\_v1}, \textit{sim\_res\_v2}), as well as between \textit{v1} and \textit{v2} themselves (\textit{sim\_v1\_v2}).

\subsection{Statistical Analysis}
\label{sec:meth-statistics}

\TODO{Detail the statistical tests used in RQ4: Pearson's chi-squared test (or Fisher's exact test for small cells), Cramér's V for categorical associations, Kruskal--Wallis and Mann--Whitney for categorical vs.\ continuous variables, with Bonferroni correction. Mention exploratory multinomial logistic regression.}

% ============================================================
% 4. Results
% ============================================================
\section{Results}
\label{sec:results}

\subsection{Dataset Overview}
\label{sec:res-overview}

\TODO{Report descriptive statistics: number of PRs with merge commits, number of merge commits, number of conflicting merges, number of conflict chunks. Break down by agent and PR outcome. Include a summary table.}

%\begin{table}
%  \caption{Dataset overview}
%  \label{tab:dataset-overview}
%  \begin{tabular}{lrrr}
%    \toprule
%    Metric & Total & Conflicting & \% \\
%    \midrule
%    PRs with merge commits & \TODO{} & --- & --- \\
%    Merge commits & \TODO{} & \TODO{} & \TODO{} \\
%    Conflict chunks & --- & \TODO{} & --- \\
%    \bottomrule
%  \end{tabular}
%\end{table}

\subsection{RQ1: Structural Nature}
\label{sec:rq1}

Our structural analysis over merge commits in agent-authored PRs reveals that the median number of conflicting chunks per failed merge is $1.0$, while the global mean is $3.85$ ($\sigma = 9.97$). These distributions are heavily right-skewed, reflecting the fact that while most merges generate a small number of chunks, there are long-tail occurrences with up to $106$ chunks in a single merge commit.
Regarding the size of the conflicting regions, both version 1 ($v1$) and version 2 ($v2$) have a median size of $2.0$ lines of code (LOC), while the mean sizes stand significantly higher at $15.93$ and $15.59$ LOC, respectively. The size of the adopted resolution similarly yields a median of $2.0$ LOC.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../results/rq1/num_chunks_global_no_outliers.png}
  \caption{Distribution of the number of conflicting chunks per merge commit (outliers excluded).}
  \label{fig:rq1_chunks}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../results/rq1/v1_loc_global_no_outliers.png}
  \caption{Distribution of $v1$ (lines of code) per conflicting chunk (outliers excluded).}
  \label{fig:rq1_v1_loc}
\end{figure}

\textbf{Contrast with Ghiotto et al.} Ghiotto et al.~\cite{ghiotto2020nature} reported an average of 1.2 chunks per file in human-authored conflicting merges, and chunk size medians around 3 to 4 LOC. Our findings suggest that AI-authored PRs tend to produce conflicting chunks that are slightly smaller in median magnitude (2 LOC) but can escalate to high mean values due to outliers, generating an average of roughly $3.8$ chunks per merge.

\subsection{RQ2: Resolution Strategies}
\label{sec:rq2}

We dynamically extracted the frequency of each strategy at the chunk level. When analyzing the raw extraction, we observe that a comparatively large fraction of chunks fell into the \texttt{Imprecise} category ($21.1\%$), indicating that context lines were frequently modified during integration, making unambiguous pattern matching difficult. 

To provide a faithful comparison with existing literature and a clearer view of the concrete strategies employed, we also report the distribution exclusively over precisely matched chunks (i.e., filtering out \texttt{Imprecise} cases). Within this precise subset, \texttt{V1} and \texttt{V2} are the most frequently adopted strategies, accounting for $28.7\%$ and $27.7\%$ of the resolutions, respectively. When the resolution synthesizes both sides organically, \texttt{Combination} accounts for $12.6\%$, while simple concatenations (\texttt{ConcatV1V2} and \texttt{ConcatV2V1}) sum up to $16.5\%$. The introduction of \texttt{New Code} during resolution accounts for $12.6\%$ of the occurrences.

Stratifying these precise outcomes by \textit{author type} (the entity executing the merge commit) reveals striking behavioral differences. When human developers step in to resolve the conflicts within these agent-authored PRs, they dominantly favor \texttt{V1}/\texttt{V2} ($57.3\%$ combined) and concatenations ($19.9\%$), resorting to \texttt{New Code} in only $9.3\%$ of chunks. Conversely, when the AI agents themselves execute the resolution natively, their reliance on \texttt{New Code} skyrockets to $25.5\%$, and \texttt{Combination} instances rise to $19.3\%$, while concatenations plummet to $2.8\%$. This suggests agents heavily favor synthesizing novel, holistic resolutions over structural patching.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../results/rq2/strategies_stacked_resolver_type_precise.png}
  \caption{Distribution of precise resolution strategies stratified by the entity that resolved the conflict.}
  \label{fig:rq2_strategies_resolver}
\end{figure}

\textbf{Contrast with Ghiotto et al.} Ghiotto et al.~\cite{ghiotto2020nature} purposefully discarded conflict chunks where the resolution could not be precisely located, meaning their metrics describe an "Imprecise-free" subset. In their human-authored landscape, \texttt{V1} and \texttt{V2} heavily dominated, comprising jointly over $64\%$ of all valid chunk resolutions, with \texttt{New Code} representing $8.7\%$, and concatenations and combinations totaling under $10\%$. By contrast, our precise AI-cohort subset shows \texttt{V1} and \texttt{V2} covering only $56.4\%$ combined. Furthermore, complex integrations (combinations and concatenations) appear globally more frequent ($\approx 29.1\%$). The stratifications indicate that both humans reacting to agent code, and agents natively resolving conflicts, induce significantly more code interleaving and unique generation than traditional human-to-human workflows.

\subsection{RQ3: Who Resolves the Conflicts?}
\label{sec:rq3}

A critical aspect of agentic workflows is defining the boundary of autonomy upon integration. Our analysis at the merge-commit level reveals that $92.6\%$ of the merge conflicts occurring within branches initiated by AI agents are resolved by \textbf{human} developers. Natively, the AI agents themselves (e.g., matching the original bot signature) resolved the conflicts independently in only $6.7\%$ of the merges, with an additional $0.6\%$ falling under an agent-assisted heuristic.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{../results/rq3/resolver_stacked_agent.png}
  \caption{Proportion of conflicts resolved natively by the AI agent versus a human developer, stratified by the original PR authoring agent.}
  \label{fig:rq3_resolver}
\end{figure}

\textbf{Summary.} This profound skew indicates that despite the high capacity of AI coding agents to author original features, conflict resolution remains overwhelmingly a human burden. Consequently, the resolution decisions observed in RQ2 predominantly reflect human remediations of AI-generated code conflicts.

% ============================================================
% 5. Discussion
% ============================================================
\section{Discussion}
\label{sec:discussion}

\subsection{Key Findings and Implications}
\label{sec:disc-findings}

\TODO{Discuss the main takeaways from RQ1--RQ4. What do the resolution patterns reveal about AI agents' merge behavior? How do they compare to known human patterns?}

\subsection{Implications for Tool Design}
\label{sec:disc-tools}

\TODO{Discuss implications for the design of AI-assisted merge tools and for CI/CD pipelines involving AI agents.}

\subsection{Collaborative Dynamics Between AI and Humans}
\label{sec:disc-collaboration}

\TODO{Discuss the interplay between AI agents and human developers in conflict resolution. When do humans step in? Does it correlate with conflict complexity?}

% ============================================================
% 6. Threats to Validity
% ============================================================
\section{Threats to Validity}
\label{sec:threats}

\subsection{Internal Validity}
\label{sec:threats-internal}

\TODO{Discuss: (1) Accuracy of resolution localization (LOCALIZERESREGION may fail for heavily modified files); (2) Strategy classification depends on whitespace normalization; (3) AI signature list may be incomplete.}

\subsection{External Validity}
\label{sec:threats-external}

\TODO{Discuss: (1) Only PRs with merge commits in the branch are included---PRs without merges (likely the majority) are excluded; (2) Integration merges are not considered; (3) Only five AI agents are covered; (4) Some repositories may be unavailable for cloning.}

\subsection{Construct Validity}
\label{sec:threats-construct}

\TODO{Discuss: (1) The eight-class taxonomy may not capture all nuances; (2) Gestalt similarity as a proxy for resolution quality; (3) Resolver = merge commit author may miss collaborative contributions.}

% ============================================================
% 7. Conclusion
% ============================================================
\section{Conclusion and Future Work}
\label{sec:conclusion}

\TODO{Summarize contributions. Highlight key findings. Outline future work directions: longitudinal analysis, integration merges, cross-agent comparison, automated resolution quality assessment.}

% ============================================================
% Artifact Availability (mandatory, unnumbered)
% ============================================================
\section*{Artifact Availability}

The dataset and scripts used in this study are publicly available at \TODO{anonymized repository URL}. The repository includes all data collection scripts, classification algorithms, and analysis notebooks. The conflict resolution content (v1, v2, base, resolution) is shared as SHA-256 hashes; full text content is available upon request due to licensing considerations.

% ============================================================
% Acknowledgements (unnumbered)
% ============================================================
\section*{Acknowledgements}

\TODO{Acknowledge funding, contributors, and AI tool usage per SBES policy (e.g., ``Tool X was used to assist with Y'').}

% ============================================================
% Bibliography
% ============================================================
\bibliographystyle{ACM-Reference-Format}
\bibliography{references}

\end{document}
